# Backend origin for the built client. Leave unset for local dev (the Vite
# proxy forwards /api to localhost:8787). Set it when deploying the client as
# a UiPath Coded App, pointing at wherever the Express server is hosted.
# VITE_API_BASE=https://your-autopilot-api.example.com
