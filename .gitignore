node_modules
dist
.env
.env.local
*.log
.DS_Store
coverage
.vite

# App binaries under test (large; pulled from the device farm on demand)
*.apk
*.aab
*.ipa

# Coded App packaging output (regenerated by `uip codedapp pack`)
.uipath/
*.nupkg

# Stale extension archive: bundles node_modules and __MACOSX cruft (~8 MB).
# The shareable build is mobile-test-autopilot-extension.zip, which is tracked.
extension.zip
